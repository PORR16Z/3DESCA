#include "DES.hpp"
#include "Bit.hpp"

int main()
{
    /*
    uint8_t n1 = 0x1;
    uint8_t n2 = 0x1;

    for (int i = 0; i < 9; i++)
    {
        n1 = Ror(n1, 2);
        n2 = Rol(n2, 1);
        printf("%d. n1=%d, n2=%d\n", i, n1, n2);
    }

    DES ciph;

    uint32_t no = 0x14C00000;// 0x16030080;// 0x34C00001; // 8000032C
    DES::chunk32 num;
    num.val=no;
//chunk48 expansion(chunk32 asd
    DES::chunk48 num2 = ciph.Expansion(num);
    //num2.arr[0] = 13;

    for (auto i : num2.arr)
        printf("row=%d\n", i);
      */
    return 0;
}